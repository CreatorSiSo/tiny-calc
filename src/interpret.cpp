#include "interpret.hpp"

auto eval_expr(unique_ptr<Expr> expr) -> double {
    // auto specific_expr = expr->downcast();
    // if (BinaryExpr** number = std::get_if<BinaryExpr>(specific_expr)) {
    // }
}
